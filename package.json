{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@stencil/sass": "^1.5.2",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build-libs": "lerna run --scope ontario-design-system* build",
    "clean": "lerna run --scope ontario-design-system* clean",
    "clean:full": "lerna run --scope ontario-design-system* clean:full"
  }
}
