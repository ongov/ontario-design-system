{
	"name": "@ontario-digital-service/ontario-design-system-design-tokens",
	"version": "0.1.0",
	"private": false,
	"description": "Design Tokens to be used in Ontario Design System",
	"main": "index.js",
	"style": "dist/css/_variables.css",
	"sass": "dist/scss/_variables.scss",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "node build.js",
		"clean": "rm -rf dist",
		"clean:full": "rm -rf node_modules && npm run clean"
	},
	"files": [
		"dist/{css,scss}/"
	],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"style-dictionary": "^3.0.3"
	}
}
